# 2026-07-08 工作日志

## 导航修复 + 清理

- 从 Header 和 Footer 中移除"招合伙人"和"供应链管理"导航项
- 修复子页面（/downloads/、/open-source/、/cases/）中导航链接失效问题：
  - 所有 Hash 锚点链接从 `#xxx` 改为 `/#xxx`（绝对路径 + 哈希）
  - 涉及：产品中心、新闻动态、关于我们、联系我们
  - Footer 资源链接改为实际页面路径（/downloads/、/open-source/、/cases/）
- 修复了移动端菜单中"联系我们"按钮的链接（`#contact` → `/#contact`）
- 构建验证通过，所有子页面导航链接均正确指向首页对应区块

## 大规模重构：精简栏目 + 产品化

### 导航精简
- Header 导航精简为 5 个：首页、产品中心 (`/products/`)、应用案例 (`/cases/`)、文档中心 (`/docs/`)、关于我们 (`/#about`)
- 移除：新闻动态、下载中心、开源共享（合并为文档中心）
- Footer 同步精简，资源区改为产品中心/应用案例/文档中心

### 新增产品 Content Collection
- `src/content/products/` — 8 篇产品 Markdown：室外网关、通信模组、开发板、云平台、室内微基站、传感器终端、边缘计算网关、物联网中台
- `/products/` 列表页 + `/products/[...slug]` 详情页（含面包屑导航）
- 首页产品卡片链接到对应产品详情页，查看全部产品指向 `/products/`

### 文档中心合并
- 将 `downloads/` (8 篇) 和 `open-source/` (8 篇) 合并为 `docs/` (16 篇)
- 每篇添加 `category: "download"` 或 `category: "open-source"` 标记
- `/docs/` 列表页带分类标签（下载/开源），`/docs/[...slug]` 详情页
- 删除旧 `src/pages/downloads/`、`src/pages/open-source/`、`src/content/downloads/`、`src/content/open-source/`

### 首页清理
- 移除 News 组件和新闻动态区块
- 应用案例区块改为预览入口，新增"查看全部案例"链接
- 删除 `src/components/News.astro`

### 内容集合
- 3 个 collection：`products`（8 篇）、`docs`（16 篇）、`cases`（8 篇）
- 构建验证：36 个页面全部静态生成成功

## Hero 区域精简

- 删除"锐米为 LoRaWAN App 登记软件著作权，让用户更好地使用该软件。"文案
- 顶部徽章改为：锐米，LoRaWAN专家 Since 2014-7-1
- 保留简短摘要：提供从模组、网关到云平台的完整 LoRaWAN 物联网解决方案。

## Hero 版式调整（品牌突出）

- 将「锐米」提升为超大主标题（5xl-8xl），「LoRaWAN专家」为副标签
- 「互利共赢 · 共创未来」降为次要标语，字号缩小、透明度降低
- 「SINCE 2014-7-1」作为顶部小字日期标注

## Hero 版式再度强化（品牌信任感）

- SINCE 2014-07-01 改为 accent 色装饰线夹持的突出徽章样式，字号从 xs→base/lg
- LoRaWAN专家 提升为独立渐变高亮行，字号从 2xl-4xl→3xl-6xl，与锐米形成双重视觉锚点
- 整体层级：日期徽章 → 锐米 → LoRaWAN专家 → 标语 → 描述

## 应用场景按截图重新设计（3×3 网格）

- 从 4 项 emoji 卡片改为 9 项图片式卡片（2×3/3×3 响应式网格）
- 9 个场景：智慧农业 / 空气质量监测 / 畜牧定位 / 工程设备管理 / 智能制造 / 液体泄露监测 / 智能仓储 / 精细农业 / 水管监测
- 每张卡片用渐变色背景 + emoji 图标 + 底部标题栏，点击跳转 /cases/
- 同步更新 cases Content Collection：删除 3 个旧案例、新增 4 个匹配案例（共 9 篇）
- config.ts 中 date 字段改为 z.coerce.date() 解决字符串日期解析问题
- 首页移除 CloudPlatform 区块
- 构建验证：37 页通过

## 首页版式重设计（节奏化布局）

### 问题诊断
- Products(白) → Scenarios(白) → About(白) 三段纯白连续无节奏
- 区块间无过渡分隔，视觉断裂
- Contact 浅灰背景太弱，缺乏行动号召力

### 改动
- **Products**: 底部加 SVG 斜切过渡（#f8fafc），指向 Scenarios 浅色背景
- **Scenarios**: 背景改为 bg-slate-50 + 细微点阵纹理，打破纯白连续感；底部加波浪过渡到 About 白底
- **About**: 顶部接收 Scenarios 波浪，添加 relative 定位承上启下
- **Contact**: 彻底重设计——深蓝渐变底（与 Hero 呼应）+ 玻璃态联系卡片 + CTA 大标题「准备好开始您的物联网项目了吗？」+ 底部波浪过渡到 Footer
- **Footer**: 配色从 gray-900 改为 slate-900 与 Contact 波浪衔接；产品链接指向真实产品页
- 最终流：Hero(深蓝) → Products(白) → Scenarios(浅灰) → About(白) → Contact(深蓝) → Footer(深色)，四段三变，节奏分明

## 应用场景图标优化

- 用户反馈 emoji 图标朦朦胧胧不清晰
- 将所有场景卡片从 emoji (`opacity-30`) 替换为 SVG 线性描边图标（白色、64x64 viewBox）
- 9 个自定义图标：植物、云/风、定位标、齿轮、工厂、水滴、仓库、监测屏、水管
- 默认 opacity-40，hover 升到 opacity-70 + 缩放，清晰锐利

## 应用场景图标回退

- SVG 图标识别度不足，改回 emoji
- emoji 透明度大幅提升：opacity-30 → opacity-80（hover 100%）
- 字号加大：3rem→3.5rem/4rem→4.5rem，加 drop-shadow-md
- 删除所有 SVG 图标代码

## 首页产品区调整

- 精简为 3 个核心硬件产品一行铺满：室外网关、通信模组、开发板
- 去掉 LoRaWAN云平台，网格从 4 列改 3 列
- 清理了不再使用的 SVG 图标代码

## 产品卡片加实拍图

- 三张产品图片复制到 `public/images/`：gateway / module / devkit
- 卡片从 SVG 图标改为实拍图（4:3 宽高比、object-contain + slate-50 底）
- hover 时图片微放大 scale-[1.02]

## 案例列表页视觉统一

- 用户反馈案例清单页风格与首页格格不入（白卡片灰底 vs 首页彩色渐变卡片）
- 完全重写 `src/pages/cases/index.astro`，不再使用共享 ListLayout
- 新设计：9 张彩色渐变卡片 + 大号 emoji + 半透明信息栏，完全对齐首页 Scenarios 区块风格
- 每张卡片按行业映射专属渐变色和 emoji（智慧农业=绿、畜牧=翡翠绿、工程设备=琥珀等）
- 顶部深蓝页面头带装饰光球，底部信息栏显示行业标签/标题/客户/日期
- hover 效果：抬升 + 阴影加强 + emoji 放大 + 右上角箭头指示器出现

## 案例列表页撤回到原结构 + 样式微调

- 用户反馈结构没问题只是样式问题，撤回彩色渐变卡片方案
- 恢复原清单结构（白卡片竖排布局），仅做样式微调：
  - 每行左侧加 emoji 圆角方块图标（与行业对应）
  - 行业标签改 primary 蓝色系（统一品牌色）
  - 右侧加标签展示
  - 阴影从 shadow-lg 降为 shadow-md

## ListLayout Header 样式统一

- 案例页 Header 从深暗渐变（800→950）+ 居中 → 改为明亮通透（600→800）+ 左对齐
- 去掉 bg-gray-50 灰色孤岛背景，内容区 -mt-8 向上交叠
- 与产品页、文档页 Header 风格完全对齐

## 官网论坛位置调整

- 从 Contact 区块底部信息栏移除「官网论坛」
- 新增到 Footer「资源支持」栏目，外链 `target="_blank"`

## 产品清单页支持产品图片

- `config.ts` products schema 新增可选 `image` 字段（`z.string().optional()`）
- 三个产品 .md 添加 `image: "/images/product-xxx.png"`
- 模板用 `set:html` + `productIcon()` 函数渲染：有图显示实拍图，无图降级 SVG 图标
- 解决 Astro 模板 `{}` 嵌套语法限制问题（从 JSX 三元表达式 → set:html）

## 文章页 Markdown 渲染修复

- 用户反馈 `#` 标题与正文样式无区别
- 安装 `@tailwindcss/typography` 插件，注册到 `tailwind.config.mjs`
- ArticleLayout.astro 的 `<article>` 加 `prose prose-slate` 类
- 修复 sensor-drivers.md category 字段（"技术白皮书"→"download"，不符合 schema 枚举）

## 新增高校合作案例

- 创建 `src/content/cases/university-cooperation.md`
- 配图：15 所高校校徽合集（北大/复旦/上交/国防科大/北邮/中大等）
- 内容：合作背景（端管云设备/源代码原理/硬件设计）+ 合作成果
- 路径：/cases/university-cooperation/

## 案例列表页 emoji 图标机制

- cases schema 新增可选 `emoji` 字段
- 清单页三级回退：emoji 字段 → 行业映射表 → 标题首字字母头像
- 高校合作案例设置 `emoji: "🎓"`

## 新增 LoRaGPS 车辆定位案例

- 创建 `src/content/cases/loragps-tracking.md`
- 客户：智云网联科技（深圳）有限公司，行业：海外IoT
- 两张配图：车辆定位场景示意图 + LoRaGPS 定位框架图
- 内容：科特迪瓦华人车辆定位刚需 + 微波联接基站方案 + 客户评价引用
- emoji: "🌍"，路径：/cases/loragps-tracking/

## 新增大连水司智能水表案例

- 创建 `src/content/cases/dalian-water.md`
- 客户：大连水司，行业：智慧水务
- 配图：LoRaWAN 智能水表架构图（大连水司/锐米通信/上海真诺三方）
- 内容：项目背景 + 孙工客户评价引用
- emoji: "💧"，路径：/cases/dalian-water/
